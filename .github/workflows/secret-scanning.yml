name: Secret Scanning

on:
  push:
    branches: [master, develop]
  pull_request:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  placeholder:
    name: Secret Scanning Pending Configuration
    runs-on: ubuntu-latest
    steps:
      - name: Pending
        run: echo "Secret scanning workflow pending Gitleaks license configuration"

  # Temporarily disabled until the repository is configured for the licensed action.
  # gitleaks:
  #   name: Gitleaks Secret Scan
  #   runs-on: ubuntu-latest
  #   continue-on-error: true
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v6.0.2
  #       with:
  #         fetch-depth: 0
  #
  #     - name: Run Gitleaks
  #       uses: gitleaks/gitleaks-action@v2
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}
  #         GITLEAKS_ENABLE_COMMENTS: true
